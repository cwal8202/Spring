파일은 filter를 가진다

이때 filter( ) 안에 람다식으로 표현이 가능하다.

walk(root, 1)

해당경로의 1단계 파일들을 보여준다.
1이면 해당경로의 파일들 목록을 다 보여준다.
2면 1단계 더 보여준다.

람다식은 그냥 메소드다.

함수를 람다식으로 표현 가능하다.

functional 프로그래밍은
함수로 프로그램을 구현하는 것


aop
관점 지향 프로그래밍
특정 시점 개입해서 전후로 작업 하게 만들면 프로그래밍 쉽겠다? 이게 뭔말이야?
AspectJ
어느 시점에 개입을 할 지를 포인트컷과 어드바이스로 표시한다.
